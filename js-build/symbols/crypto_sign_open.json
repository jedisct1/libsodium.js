{
	"name": "crypto_sign_open",
	"dependencies": ["_crypto_sign_publickeybytes", "_crypto_sign_bytes"],
	"type": "function",
	"inputs": [
		{
			"name": "signedMessage",
			"type": "unsized_buf"
		},
		{
			"name": "publicKey",
			"type": "buf",
			"size": "libsodium._crypto_sign_publickeybytes()"
		}
	],
	"outputs": [
		{
			"type": "buf",
			"name": "message",
			"size": "signedMessage_size - libsodium._crypto_sign_bytes()"
		}
	],
	"target": "libsodium._crypto_sign_open(message_byte, message_size, signedMessage_byte, signedMessage_size, 0, publicKey_byte) | 0",
	"expect": "=== 0",
	"return": "formatOutput(message, outputFormat)"
}
